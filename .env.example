# Copie para .env e ajuste conforme o ambiente.
# Com docker compose, estes valores já vêm do compose e o .env é opcional.

DB_HOST=db
DB_PORT=3306
DB_NAME=sistema_lua
DB_USER=sistema
DB_PASS=sistema

APP_DEBUG=0
